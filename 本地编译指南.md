# NoxKVM 本地编译指南

本文档介绍如何在本地环境中编译 NoxKVM (JetKVM) 项目，包括设置交叉编译工具链和完整的编译流程。

## 📋 目录

- [前提条件](#前提条件)
- [设置交叉编译工具链](#设置交叉编译工具链)
- [编译项目](#编译项目)
- [编译选项](#编译选项)
- [常见问题](#常见问题)
- [部署到设备](#部署到设备)

---

## 前提条件

### 系统要求

- **操作系统**: Linux 或 macOS（Windows 推荐使用 WSL）
- **架构**: x86_64（用于交叉编译到 ARM）

### 必需软件

1. **Go 1.24.4+** - [下载安装](https://go.dev/doc/install)
2. **Node.js 22.21.1** - [下载安装](https://nodejs.org/en/download/)
3. **Git** - 版本控制
4. **CMake** - 构建系统
5. **GCC/G++** - C/C++ 编译器

### 验证安装

```bash
go version
node --version
npm --version
cmake --version
gcc --version
```

---

## 设置交叉编译工具链

NoxKVM 需要交叉编译到 ARM 架构（Rockchip RV1106）。你需要下载并安装专用的交叉编译工具链。

### 方法一：使用自动化脚本（推荐）

项目提供了一个一键安装脚本 `setup_toolchain.sh`：

```bash
cd ~/NoxKVM
chmod +x setup_toolchain.sh
sudo ./setup_toolchain.sh
```

该脚本会自动：
1. 安装所有系统依赖包
2. 下载交叉编译工具链（v0.2.5）
3. 安装到 `/opt/jetkvm-native-buildkit`

### 方法二：手动安装

#### 步骤 1：安装系统依赖

```bash
sudo apt-get update
sudo apt-get install -y --no-install-recommends \
  iputils-ping \
  build-essential \
  device-tree-compiler \
  gperf \
  gdb-multiarch \
  libnl-3-dev \
  libdbus-1-dev \
  libelf-dev \
  libmpc-dev \
  dwarves \
  bc \
  openssl \
  flex \
  bison \
  libssl-dev \
  python3 \
  python-is-python3 \
  texinfo \
  kmod \
  cmake \
  wget \
  zstd \
  python3-venv \
  python3-kconfiglib \
  g++-multilib \
  gcc-multilib
```

#### 步骤 2：下载交叉编译工具链

```bash
# 创建临时目录
BUILDKIT_TMPDIR=$(mktemp -d)
cd "$BUILDKIT_TMPDIR"

# 下载工具链
wget https://github.com/jetkvm/rv1106-system/releases/download/v0.2.5/buildkit.tar.zst

# 创建安装目录
sudo mkdir -p /opt/jetkvm-native-buildkit

# 解压工具链
sudo tar --use-compress-program="unzstd --long=31" -xvf buildkit.tar.zst -C /opt/jetkvm-native-buildkit

# 清理临时文件
rm buildkit.tar.zst
cd ~
rm -rf "$BUILDKIT_TMPDIR"
```

#### 步骤 3：验证安装

```bash
# 检查工具链是否正确安装
ls -la /opt/jetkvm-native-buildkit/

# 应该能看到 rv1106-jetkvm-v2.cmake 文件
ls -la /opt/jetkvm-native-buildkit/rv1106-jetkvm-v2.cmake

# 检查交叉编译器
ls -la /opt/jetkvm-native-buildkit/bin/
```

---

## 编译项目

### 首次编译

首次编译需要先构建前端（生成 `static` 目录），然后编译整个项目：

```bash
cd ~/NoxKVM

# 1. 构建前端（生成 static 目录）
make frontend

# 2. 编译完整项目
make build_dev
```

### 编译输出

编译成功后，会在 `bin/` 目录生成以下文件：

- **`bin/jetkvm_app`** - 主程序二进制文件（约 37MB）
  - 架构：ARM 32-bit EABI5
  - 类型：动态链接，使用 uClibc
  - 适用于 JetKVM 设备（Rockchip RV1106）

---

## 编译选项

### 常用编译命令

| 命令 | 说明 |
|------|------|
| `make build_dev` | 编译开发版本（包含调试信息） |
| `make build_release` | 编译发布版本 |
| `make frontend` | 仅编译前端（React UI） |
| `make build_native` | 仅编译原生库（CGO/C 代码） |
| `make test` | 运行所有测试 |
| `make lint` | 代码检查 |
| `make check` | 运行 lint + test |

### 编译标志

你可以通过环境变量控制编译过程：

```bash
# 跳过前端构建（加快编译速度）
make build_dev SKIP_UI_BUILD=1

# 跳过原生库构建（如果已存在）
make build_dev SKIP_NATIVE_IF_EXISTS=1

# 跳过前端和原生库（仅编译 Go 代码）
make build_dev SKIP_UI_BUILD=1 SKIP_NATIVE_IF_EXISTS=1

# 启用同步追踪（调试用）
make build_dev ENABLE_SYNC_TRACE=1

# 指定 CMake 构建类型（Debug/Release）
make build_native CMAKE_BUILD_TYPE=Debug
```

### 前端开发模式

如果你只修改前端代码，可以使用热重载的开发服务器：

```bash
cd ui
npm ci
./dev_device.sh <设备IP地址>

# 例如：
./dev_device.sh 192.168.1.100
```

这会在本地启动 Vite 开发服务器，支持实时预览。

---

## 常见问题

### 问题 1：`pattern all:static: no matching files found`

**原因**: 缺少前端静态文件目录。

**解决方案**:
```bash
# 先构建前端
make frontend

# 然后再编译
make build_dev
```

### 问题 2：Docker 镜像拉取失败

**错误信息**:
```
Unable to find image 'ghcr.io/jetkvm/buildkit:latest' locally
docker: Error response from daemon: error from registry: denied
```

**原因**: 远程 Docker 镜像需要认证。

**解决方案**: 使用本地交叉编译工具链（见上文"设置交叉编译工具链"部分），这样就不需要 Docker 了。

### 问题 3：Node.js 版本不匹配

**警告信息**:
```
npm warn EBADENGINE Unsupported engine {
  package: 'kvm-ui@2025.12.11.1200',
  required: { node: '^22.21.1' },
  current: { node: 'v24.12.0', npm: '11.6.2' }
}
```

**说明**: 项目推荐使用 Node.js 22.21.1，但 24.x 版本通常也能正常工作。如果遇到构建问题，可以：

```bash
# 使用 nvm 安装推荐版本
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 22.21.1
nvm use 22.21.1
```

### 问题 4：原生库符号链接问题

**错误信息**:
```
fatal error: ui/ui.h: No such file or directory
```

**原因**: Git 符号链接未正确创建。

**解决方案**:
```bash
# 启用 Git 符号链接
git config core.symlinks true
git restore internal/native/cgo/ui

# 或手动创建符号链接
cd internal/native/cgo
rm -f ui
ln -s ../eez/src/ui ui
```

### 问题 5：清理构建缓存

如果遇到不稳定的构建问题：

```bash
# 清理前端缓存
cd ui && rm -rf node_modules/ && npm ci && cd ..

# 清理 Go 缓存
go clean -modcache
go mod tidy

# 清理原生库构建
rm -rf internal/native/cgo/build

# 重新编译
make build_dev
```

---

## 部署到设备

### 使用部署脚本

如果你有 JetKVM 设备，可以使用自动化部署脚本：

```bash
# 基本部署（编译 + 部署）
./dev_deploy.sh -r <设备IP地址>

# 例如：
./dev_deploy.sh -r 192.168.1.100
```

### 部署选项

| 选项 | 说明 |
|------|------|
| `-r <IP>` | 设备 IP 地址（必需） |
| `-u <user>` | 远程用户名（默认：root） |
| `--skip-ui-build` | 跳过前端构建 |
| `--skip-native-build` | 跳过原生库构建 |
| `--run-go-tests` | 运行 Go 测试 |
| `--enable-sync-trace` | 启用同步追踪 |
| `--log-trace <scopes>` | 启用日志追踪（如：usb,hidrpc,all） |
| `--native-binary` | 仅构建和部署原生二进制（调试用） |

### 示例

```bash
# 快速部署（跳过前端）
./dev_deploy.sh -r 192.168.1.100 --skip-ui-build

# 部署并运行测试
./dev_deploy.sh -r 192.168.1.100 --run-go-tests

# 启用 USB 调试日志
./dev_deploy.sh -r 192.168.1.100 --log-trace "usb,hidrpc"
```

### 验证部署

部署完成后：

1. **访问设备 Web 界面**: `http://<设备IP>`
2. **查看设备日志**:
   ```bash
   ssh root@<设备IP>
   tail -f /userdata/jetkvm/last.log
   ```

---

## 项目结构

```
NoxKVM/
├── bin/                    # 编译输出目录
│   └── jetkvm_app         # 主程序二进制
├── cmd/                    # 程序入口
├── internal/               # 内部 Go 包
│   ├── native/            # CGO/原生代码
│   │   └── cgo/           # C 代码和库
│   └── ...
├── ui/                     # React 前端
│   └── src/               # 源代码
├── static/                 # 前端构建输出（自动生成）
├── scripts/                # 构建脚本
│   ├── build_cgo.sh       # 编译原生库
│   └── build_utils.sh     # 构建工具函数
├── setup_toolchain.sh      # 工具链安装脚本
└── Makefile               # 主构建文件
```

---

## 高级主题

### 交叉编译工具链说明

工具链位于 `/opt/jetkvm-native-buildkit`，包含：

- **交叉编译器**: `arm-rockchip830-linux-uclibcgnueabihf-gcc`
- **CMake 工具链文件**: `rv1106-jetkvm-v2.cmake`
- **目标库**: Rockchip MPP、RGA、Rockit 等

Makefile 会自动检测并使用这些工具：

```makefile
BUILDKIT_PATH ?= /opt/jetkvm-native-buildkit
BUILDKIT_FLAVOR := arm-rockchip830-linux-uclibcgnueabihf

# 如果工具链存在，启用 CGO
ifneq ($(wildcard $(BUILDKIT_PATH)),)
    CGO_ENABLED=1
    CC="$(BUILDKIT_PATH)/bin/$(BUILDKIT_FLAVOR)-gcc"
    CGO_CFLAGS="-I$(BUILDKIT_PATH)/$(BUILDKIT_FLAVOR)/include ..."
    CGO_LDFLAGS="-L$(BUILDKIT_PATH)/$(BUILDKIT_FLAVOR)/lib ..."
endif
```

### 编译参数说明

Go 编译参数：

```bash
GOOS=linux              # 目标操作系统
GOARCH=arm              # 目标架构
GOARM=7                 # ARM 版本
CGO_ENABLED=1           # 启用 CGO

# 链接器标志
-ldflags="-s -w         # 去除调试信息，减小体积
  -X ...Branch=dev      # Git 分支
  -X ...BuildDate=...   # 构建时间
  -X ...Revision=...    # Git 提交哈希
  -X ...Version=..."    # 版本号
```

---

## 参考资源

- [JetKVM 官方文档](https://jetkvm.com/docs)
- [开发指南](DEVELOPMENT.md)
- [GitHub 仓库](https://github.com/jetkvm/kvm)
- [Discord 社区](https://jetkvm.com/discord)

---

## 更新日志

- **2026-04-11**: 初始版本，添加完整的本地编译指南
  - 交叉编译工具链设置
  - 前端和后端编译流程
  - 常见问题解决方案
