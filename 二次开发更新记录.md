# NoxKVM 二次开发更新记录

本文档记录 NoxKVM fork 相对于上游 [jetkvm/kvm](https://github.com/jetkvm/kvm) 的所有二次开发变更。

基准分支：`upstream/dev`

---

## OTA 与发布

- **OTA 更新源迁移至腾讯云 COS**：`DefaultAPIURL` 从 `https://api.jetkvm.com` 改为 `https://dl.acenova.tech/noxkvm/app/latest`（`config.go`）
- **GPG 签名密钥替换**：OTA 验证根密钥指纹替换为 NoxKVM 自签名密钥 `0A26B9F222A203471A1DA1F3BBD2E045E4F95170`（`internal/ota/gpg.go`）
- **新增 CI/CD 发布 workflow**：`.github/workflows/release.yml`，构建签名版二进制、创建 GitHub Release、上传至腾讯云 COS、部署 OTA 元数据
- **COS 上传优化**：使用 coscli 替代 coscmd，支持分片上传、重试机制、桶内服务端拷贝（`latest/` 路径免流量拷贝）
- **自定义 CDN 域名分发**：设备 OTA 元数据及固件包通过自定义域名 `dl.acenova.tech` 分发
- **自动触发索引更新**：release 完成后自动 dispatch `cos-index-repo` 更新
- **OTA 元数据支持系统固件更新**：release 文件动态获取固件仓库元数据，自动填充 `systemVersion`、`systemUrl`、`systemHash` 字段

## 代码变更

### 视频码率

- **移除 REMB 动态码率调节**：删除 `video_bitrate.h`、`video_remb.go`、`video_remb_test.go` 及相关 gRPC/protobuf 接口（`VideoSetREMB`），简化码率为静态计算
- **码率计算重构**：将 `video_bitrate_for()` 的 REMB 感知逻辑替换为纯分辨率缩放的 `calculate_bitrate()` 函数（`internal/native/cgo/video.c`）
- **质量因子默认值**：`VideoQualityFactor` 默认从 `0`（Auto/REMB）改为 `1.0`（High）
- **质量因子验证放宽**：允许大于 1 的值通过验证（`internal/native/cgo/ctrl.c`、`internal/native/native.go`）
- **移除 RTCP REMB 读取**：`readVideoRTCP` 重命名为 `drainRTCP`，不再解析 REMB 反馈（`webrtc.go`）
- **移除 video_during_update 能力声明**：`rpcGetDeviceCapabilities` 不再返回 `video_during_update`（`jsonrpc.go`）

### USB 与存储

- **镜像存储路径修改**：USB 大容量存储镜像目录从 `/userdata/jetkvm/images` 改为 `/mnt/sdcard`（`usb_mass_storage.go`）

### USB Gadget

- **移除音频音量控制配置**：删除 `c_mute_present`、`c_volume_present`、`p_mute_present`、`p_volume_present` 等 ALSA mixer 配置项（`internal/usbgadget/audio.go`）

### 依赖

- **MQTT 依赖提升**：`github.com/eclipse/paho.mqtt.golang` 从 indirect 提升为直接依赖（`go.mod`）

## 文档

- **中文 README 重写**：补充 NoxKVM fork 说明、发布流程、代码改动详情
- **本地编译指南**：新增交叉编译环境配置脚本 `setup_toolchain.sh` 及编译指南文档
- **WebRTC ICE 卡住解决方案**：记录 ICE gathering completed 后连接失败的排查方案
- **删除文件**：`config_video_bitrate_test.go`、`scripts/test_video_bitrate.sh`、`ui/e2e/video-quality.spec.ts`、`ui/src/components/VideoOverlay.tsx`、`ui/src/hooks/useHiddenVideoStreamPause.ts`
